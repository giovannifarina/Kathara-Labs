# 1. Crea l'entità bridge virtuale
ip link add name br0 type bridge

# 2. Collega le interfacce fisiche al bridge (le rende "schiave")
ip link set eth0 master br0
ip link set eth1 master br0

# 3. Accendi tutto
ip link set eth0 up
ip link set eth1 up
ip link set br0 up